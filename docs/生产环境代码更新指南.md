# 生产环境代码更新指南

本文档说明在生产服务器上更新代码时需要注意的事项和最佳实践。

## 📋 更新前检查清单

### 1. 备份数据

在更新代码之前，**务必**先备份重要数据：

```bash
# 1. 备份数据库
cp data/paper_push.db data/paper_push.db.backup.$(date +%Y%m%d_%H%M%S)

# 2. 备份配置文件
cp .env .env.backup.$(date +%Y%m%d_%H%M%S)

# 3. 备份整个项目（可选）
cd /path/to
tar -czf bio_backup_$(date +%Y%m%d_%H%M%S).tar.gz bio/

# 4. 备份定时任务配置
crontab -l > crontab_backup_$(date +%Y%m%d_%H%M%S).txt
```

### 2. 检查当前运行状态

```bash
# 检查是否有任务正在运行
ps aux | grep "python.*backend\|python.*app"

# 检查定时任务状态（crontab）
crontab -l

# 检查定时任务状态（systemd）
sudo systemctl status paper-push.timer
sudo systemctl status paper-push.service

# 查看最近的日志
tail -50 logs/paper_push_*.log
```

### 3. 查看更新内容

```bash
# 查看当前版本
git log -1 --oneline

# 查看将要更新的内容
git fetch origin
git log HEAD..origin/main --oneline  # 或其他分支名

# 查看具体更改
git fetch origin
git diff HEAD..origin/main
```

### 4. 检查依赖变化

```bash
# 查看 requirements.txt 是否有变化
git diff HEAD..origin/main requirements.txt

# 如果有变化，记录下来，更新后需要重新安装
```

## 🚀 代码更新步骤

### 方式一：使用 Git 更新（推荐）

```bash
# 1. 进入项目目录
cd /path/to/bio

# 2. 停止正在运行的服务（如果使用 systemd）
sudo systemctl stop paper-push.timer
sudo systemctl stop paper-push.service

# 或者确保没有定时任务正在执行（如果使用 crontab）
# 等待当前任务完成，或者临时禁用 crontab

# 3. 拉取最新代码
git fetch origin
git pull origin main  # 或其他分支名

# 4. 检查是否有冲突
git status

# 5. 如果有依赖更新，重新安装依赖
source venv/bin/activate  # 如果使用虚拟环境
pip install -r requirements.txt

# 6. 检查配置文件是否需要更新
# 对比 .env.example 和 .env，看是否有新的配置项
diff .env.example .env

# 7. 运行测试（如果有）
python -m pytest tests/  # 可选

# 8. 手动运行一次，验证功能
python -m backend run --help  # 先看帮助
python -m backend test-sources  # 测试数据源连通性

# 9. 重新启动服务
sudo systemctl start paper-push.timer
sudo systemctl start paper-push.service

# 或重新启用 crontab（如果临时禁用了）

# 10. 查看日志确认正常运行
tail -f logs/paper_push_*.log
```

### 方式二：手动上传文件更新

如果无法使用 Git：

```bash
# 1. 在本地打包新代码（排除不需要的文件）
tar -czf bio_update.tar.gz \
  --exclude='data' \
  --exclude='logs' \
  --exclude='reports' \
  --exclude='.git' \
  --exclude='venv' \
  --exclude='__pycache__' \
  --exclude='.env' \
  backend/ frontend/ app/ docker/ scripts/ requirements.txt pyproject.toml

# 2. 上传到服务器
scp bio_update.tar.gz user@server:/tmp/

# 3. 在服务器上解压
cd /path/to/bio
tar -xzf /tmp/bio_update.tar.gz

# 4. 按照方式一的步骤 4-10 继续执行
```

## ⚠️ 关键注意事项

### 1. 不要直接删除或覆盖数据

- **永远不要**删除 `data/` 目录（包含数据库）
- **永远不要**删除 `logs/` 目录（包含日志）
- **永远不要**删除 `reports/` 目录（包含生成的报告）
- **永远不要**覆盖 `.env` 配置文件

### 2. 避免在任务运行时更新

```bash
# 查看定时任务计划
crontab -l

# 例如：如果设置的是每天 8:30 运行
# 30 8 * * * cd /path/to/bio && python -m backend run

# 建议在任务运行前或运行后更新代码
# 避免在 8:30-9:00 之间更新（任务可能正在执行）
```

### 3. 处理数据库迁移

如果代码更新涉及数据库结构变化：

```bash
# 1. 先备份数据库
cp data/paper_push.db data/paper_push.db.backup

# 2. 查看代码中是否有数据库迁移脚本
ls scripts/*migrate* scripts/*migration*

# 3. 如果有迁移脚本，按照说明执行
# 4. 如果没有，代码通常会自动处理（检查日志确认）
```

### 4. 环境变量更新

```bash
# 查看是否有新的环境变量
diff .env.example .env

# 如果有新增配置项，需要添加到 .env
nano .env  # 或 vim .env

# 常见的新增配置项：
# - 新的 API 密钥
# - 新的数据源配置
# - 新的推送渠道配置
# - 性能优化参数
```

### 5. 前端代码更新（如果使用 Web 界面）

```bash
# 如果更新了前端代码
cd frontend

# 1. 安装新依赖（如果 package.json 有变化）
npm install

# 2. 重新构建（生产环境）
npm run build

# 3. 如果使用 Docker，需要重建容器
cd ../docker
docker-compose down
docker-compose build
docker-compose up -d
```

## 🔄 零停机更新（高级）

对于需要持续运行的服务，可以使用零停机更新策略：

### 使用 Docker

```bash
# 1. 构建新镜像
cd docker
docker-compose build

# 2. 滚动更新
docker-compose up -d --no-deps --build backend

# 3. 验证新容器正常运行
docker-compose logs -f backend

# 4. 如果有问题，快速回滚
docker-compose down
docker-compose up -d
```

### 使用多实例

```bash
# 1. 启动一个新实例用于测试
cd /path/to/bio_test
git pull
# ... 更新步骤 ...

# 2. 测试新实例
python -m backend test-sources

# 3. 如果测试通过，更新生产实例
cd /path/to/bio
git pull
# ... 更新步骤 ...
```

## 📊 更新后验证

### 1. 功能验证

```bash
# 测试数据源连通性
python -m backend test-sources

# 手动运行一次完整流程
python -m backend run

# 检查是否有错误
tail -100 logs/paper_push_*.log | grep ERROR
```

### 2. 监控日志

```bash
# 实时查看日志
tail -f logs/paper_push_*.log

# 查看错误日志
grep -i error logs/paper_push_*.log

# 查看警告
grep -i warning logs/paper_push_*.log
```

### 3. 验证定时任务

```bash
# 检查 crontab 是否正常
crontab -l

# 查看 cron 执行日志
grep CRON /var/log/syslog | tail -20

# 或检查 systemd timer
sudo systemctl status paper-push.timer
sudo systemctl list-timers | grep paper-push
```

### 4. 数据库完整性检查

```bash
# 连接数据库检查
sqlite3 data/paper_push.db "SELECT COUNT(*) FROM papers;"
sqlite3 data/paper_push.db "SELECT COUNT(*) FROM runs;"

# 查看最近的运行记录
sqlite3 data/paper_push.db "SELECT id, start_time, status FROM runs ORDER BY start_time DESC LIMIT 5;"
```

## 🔙 回滚方案

如果更新后出现问题，需要快速回滚：

### 方式一：Git 回滚

```bash
# 1. 停止服务
sudo systemctl stop paper-push.timer

# 2. 回滚到上一个版本
git log --oneline -10  # 查看历史版本
git reset --hard <commit-hash>  # 回滚到指定版本

# 3. 恢复依赖（如果需要）
pip install -r requirements.txt

# 4. 恢复数据库（如果需要）
cp data/paper_push.db.backup data/paper_push.db

# 5. 重启服务
sudo systemctl start paper-push.timer

# 6. 验证
tail -f logs/paper_push_*.log
```

### 方式二：使用备份恢复

```bash
# 1. 停止服务
sudo systemctl stop paper-push.timer

# 2. 恢复整个项目
cd /path/to
mv bio bio_failed
tar -xzf bio_backup_YYYYMMDD_HHMMSS.tar.gz

# 3. 恢复数据库（使用最新的备份）
cd bio
cp data/paper_push.db.backup.YYYYMMDD_HHMMSS data/paper_push.db

# 4. 恢复配置文件
cp .env.backup.YYYYMMDD_HHMMSS .env

# 5. 重启服务
sudo systemctl start paper-push.timer
```

## 🐛 常见问题处理

### 问题1：依赖冲突

```bash
# 症状：pip install 报错
# 解决：
pip install --upgrade pip
pip install -r requirements.txt --force-reinstall
```

### 问题2：数据库锁定

```bash
# 症状：database is locked
# 解决：
# 1. 停止所有访问数据库的进程
ps aux | grep python | grep backend
kill <pid>

# 2. 检查数据库文件
ls -lah data/paper_push.db*

# 3. 如果有 .db-wal 或 .db-shm 文件，等待其释放
```

### 问题3：权限错误

```bash
# 症状：Permission denied
# 解决：
chmod 755 data logs reports
chown -R your-user:your-group data logs reports
```

### 问题4：模块找不到

```bash
# 症状：ModuleNotFoundError
# 解决：
# 1. 确保在正确的目录
pwd

# 2. 确保虚拟环境已激活（如果使用）
which python
source venv/bin/activate

# 3. 重新安装依赖
pip install -r requirements.txt
```

### 问题5：API 密钥失效

```bash
# 症状：401 Unauthorized 或 API key invalid
# 解决：
# 1. 检查 .env 文件
cat .env | grep API_KEY

# 2. 更新 API 密钥
nano .env

# 3. 测试 API 连接
python -m backend test-sources
```

## 📝 更新检查脚本

创建一个自动检查脚本 `scripts/check_update_safety.sh`：

```bash
#!/bin/bash
# 代码更新前安全检查脚本

echo "=== 代码更新前安全检查 ==="
echo ""

# 1. 检查是否有进程在运行
echo "1. 检查运行中的进程..."
if ps aux | grep -q "[p]ython.*backend\|[p]ython.*app"; then
    echo "⚠️  警告：检测到 Python 进程正在运行！"
    ps aux | grep "[p]ython.*backend\|[p]ython.*app"
    echo "建议：等待进程结束或手动停止"
else
    echo "✅ 没有进程运行"
fi
echo ""

# 2. 检查数据库备份
echo "2. 检查数据库备份..."
if [ -f "data/paper_push.db.backup.$(date +%Y%m%d)"* ]; then
    echo "✅ 今天的数据库备份已存在"
else
    echo "⚠️  警告：今天还没有备份数据库！"
    echo "建议：运行 cp data/paper_push.db data/paper_push.db.backup.$(date +%Y%m%d_%H%M%S)"
fi
echo ""

# 3. 检查磁盘空间
echo "3. 检查磁盘空间..."
DISK_USAGE=$(df -h . | tail -1 | awk '{print $5}' | sed 's/%//')
if [ "$DISK_USAGE" -gt 90 ]; then
    echo "⚠️  警告：磁盘使用率 ${DISK_USAGE}%，空间不足！"
else
    echo "✅ 磁盘空间充足 (使用率: ${DISK_USAGE}%)"
fi
echo ""

# 4. 检查 Git 状态
echo "4. 检查 Git 状态..."
if git diff-index --quiet HEAD --; then
    echo "✅ 没有未提交的更改"
else
    echo "⚠️  警告：有未提交的更改！"
    git status -s
fi
echo ""

# 5. 检查即将更新的内容
echo "5. 检查待更新内容..."
git fetch origin -q
COMMITS=$(git log HEAD..origin/main --oneline | wc -l)
if [ "$COMMITS" -gt 0 ]; then
    echo "📦 有 $COMMITS 个新提交待更新："
    git log HEAD..origin/main --oneline
else
    echo "✅ 已是最新版本"
fi
echo ""

# 6. 检查依赖变化
echo "6. 检查依赖变化..."
if git diff HEAD..origin/main requirements.txt | grep -q "^+\|^-"; then
    echo "⚠️  requirements.txt 有变化，需要重新安装依赖"
    git diff HEAD..origin/main requirements.txt
else
    echo "✅ 依赖无变化"
fi
echo ""

echo "=== 检查完成 ==="
```

使用方法：

```bash
chmod +x scripts/check_update_safety.sh
./scripts/check_update_safety.sh
```

## 📚 最佳实践总结

1. **更新前三要素**：备份、停止、测试
2. **更新时机**：避开定时任务执行时间
3. **更新顺序**：依赖 → 代码 → 配置 → 测试
4. **更新验证**：日志 → 功能 → 数据库
5. **快速回滚**：保留备份，记录版本

## 🔗 相关文档

- [服务器部署指南](./服务器部署指南.md) - 首次部署完整指南
- [部署调整清单](./部署调整清单.md) - 配置调整检查清单
- [README.md](../README.md) - 项目概述和使用说明

---

**记住**：谨慎操作，备份为先，出现问题立即回滚！

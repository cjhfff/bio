# 生产环境代码更新指南

本文档说明如何安全地更新已经在服务器上运行的代码，避免服务中断和数据丢失。

> **💡 快速指南**：如果您需要快速查看常见场景的操作步骤，请参考 [常见更新场景快速指南](./常见更新场景快速指南.md)。
> 
> **📋 检查清单**：可打印的检查清单请参考项目根目录的 [代码更新检查清单.txt](../代码更新检查清单.txt)。

## 📋 更新前检查清单

在开始更新之前，请确认以下事项：

### 1. 了解本次更新内容
- ✅ 查看更新日志/提交记录，了解本次更新了哪些功能
- ✅ 确认是否有**破坏性变更**（API 变化、配置文件格式变化等）
- ✅ 确认是否需要**新的环境变量**或配置
- ✅ 确认是否需要**数据库迁移**

```bash
# 查看最近的提交记录
git log --oneline -10

# 查看具体的代码变更
git diff <old-commit> <new-commit>
```

### 2. 检查当前运行状态
- ✅ 确认系统当前运行正常
- ✅ 记录当前版本号/提交哈希
- ✅ 检查当前的定时任务状态

```bash
# 记录当前版本
git log -1 > /tmp/version_before_update.txt

# 检查定时任务状态
systemctl status paper-push.timer  # 如果使用 systemd
# 或
crontab -l  # 如果使用 crontab

# 检查后端服务状态（如果使用 Docker）
cd docker && docker-compose ps
```

### 3. 选择合适的更新时间
- ✅ 避开业务高峰期
- ✅ 确保有足够时间处理可能出现的问题
- ✅ 通知相关人员（如果有的话）

---

## 🔒 第一步：备份（重要！）

**永远不要跳过备份步骤！**

### 1. 备份数据库

```bash
# 创建备份目录
mkdir -p /root/bio_backups/$(date +%Y%m%d_%H%M%S)
BACKUP_DIR=/root/bio_backups/$(date +%Y%m%d_%H%M%S)

# 备份 SQLite 数据库
cp data/database/paper_push.db $BACKUP_DIR/

# 验证备份
ls -lh $BACKUP_DIR/paper_push.db
```

### 2. 备份配置文件

```bash
# 备份环境变量配置
cp .env $BACKUP_DIR/

# 备份其他配置文件（如果有）
cp backend/config.py $BACKUP_DIR/ 2>/dev/null || true
```

### 3. 备份当前代码版本

```bash
# 记录当前 Git 提交
git log -1 > $BACKUP_DIR/git_commit.txt
git diff > $BACKUP_DIR/local_changes.diff

# 如果有本地修改，打包整个项目
cd ..
tar -czf $BACKUP_DIR/bio_full_backup.tar.gz bio/ --exclude=bio/data --exclude=bio/logs --exclude=bio/__pycache__
```

### 4. 导出重要数据（可选）

```bash
# 导出最近的论文数据（可选）
sqlite3 data/database/paper_push.db ".dump papers" > $BACKUP_DIR/papers_dump.sql
sqlite3 data/database/paper_push.db ".dump runs" > $BACKUP_DIR/runs_dump.sql
```

---

## 🔄 第二步：停止运行中的服务

### 如果使用 Docker：

```bash
cd docker
docker-compose down
```

### 如果使用 Systemd：

```bash
# 停止定时器
sudo systemctl stop paper-push.timer

# 确认定时器已停止
sudo systemctl status paper-push.timer

# 检查是否有正在运行的任务
ps aux | grep "python.*backend"
# 如果有，手动杀死进程
# kill <PID>
```

### 如果使用 Crontab：

```bash
# 暂时禁用定时任务
crontab -l > /tmp/crontab_backup.txt
crontab -r  # 移除所有定时任务（更新后再恢复）

# 或者注释掉相关行
crontab -e
# 在相关行前加 #

# 检查是否有正在运行的任务
ps aux | grep "python.*backend"
```

### 如果使用后台进程（nohup）：

```bash
# 查找进程
ps aux | grep "uvicorn backend.api.main"

# 杀死进程
pkill -f "uvicorn backend.api.main"

# 或使用 PID
kill <PID>
```

---

## 📥 第三步：更新代码

### 方式1：使用 Git（推荐）

```bash
# 确保在项目目录
cd /path/to/bio

# 获取最新代码
git fetch origin

# 查看将要拉取的变更
git log HEAD..origin/main --oneline

# 拉取最新代码
git pull origin main

# 如果有冲突，解决冲突后：
# git add .
# git commit -m "Merge updates"
```

### 方式2：使用 SCP 上传

```bash
# 在本地执行
scp -r bio/ user@server:/path/to/new_bio_temp

# 在服务器上
cd /path/to
mv bio bio_old_$(date +%Y%m%d)
mv new_bio_temp bio
```

### 方式3：手动替换特定文件

```bash
# 只更新特定文件
scp backend/api/main.py user@server:/path/to/bio/backend/api/
scp backend/core/scorer.py user@server:/path/to/bio/backend/core/
```

---

## 🔧 第四步：更新依赖和配置

### 1. 更新 Python 依赖

```bash
# 激活虚拟环境（如果使用）
source venv/bin/activate

# 更新依赖
pip install -r requirements.txt --upgrade

# 验证关键依赖
python -c "import backend; print('Backend module loaded successfully')"
```

### 2. 检查环境变量配置

```bash
# 对比新旧配置文件
diff .env.example .env

# 如果有新增的环境变量，添加到 .env
nano .env
```

**常见的新增配置项**：
- 新的 API 密钥
- 新的功能开关
- 性能调优参数

### 3. 数据库迁移（如果需要）

```bash
# 如果有数据库结构变更，运行迁移脚本
python scripts/migrate_database.py  # 示例

# 或者手动执行 SQL
sqlite3 data/database/paper_push.db < scripts/migration_xxx.sql
```

### 4. 创建新目录（如果需要）

```bash
# 确保所有必需的目录存在
mkdir -p data/database data/cache logs reports
```

---

## ✅ 第五步：测试验证

**不要急于重启服务，先测试！**

### 1. 运行基础测试

```bash
# 测试代码能否正常加载
python -c "from backend.api.main import app; print('✓ Backend loaded')"

# 测试数据源连接
python -m backend test-sources

# 运行单元测试（如果有）
pytest tests/ -v
```

### 2. 手动运行一次完整流程

```bash
# 运行一次完整的数据抓取和推送
python -m backend run --window-days 1 --top-k 5

# 检查日志
tail -100 logs/paper_push_*.log

# 检查生成的报告
ls -lh reports/
```

### 3. 检查输出结果

- ✅ 日志中没有错误
- ✅ 数据库中有新数据
- ✅ 报告文件生成成功
- ✅ 推送功能正常（如果启用）

### 4. 测试 Web 界面（如果有）

```bash
# 启动后端 API（测试模式）
python -m uvicorn backend.api.main:app --reload --port 8001

# 在浏览器访问
# http://server-ip:8001/docs

# 测试前端（如果需要）
cd frontend
npm install
npm run dev
```

---

## 🚀 第六步：重启服务

### 如果使用 Docker：

```bash
cd docker
docker-compose up -d

# 查看日志
docker-compose logs -f backend
```

### 如果使用 Systemd：

```bash
# 重新加载配置（如果修改了 service 文件）
sudo systemctl daemon-reload

# 启动定时器
sudo systemctl start paper-push.timer

# 确认状态
sudo systemctl status paper-push.timer

# 查看下次运行时间
systemctl list-timers paper-push.timer
```

### 如果使用 Crontab：

```bash
# 恢复定时任务
crontab /tmp/crontab_backup.txt

# 或重新编辑
crontab -e

# 确认定时任务已添加
crontab -l
```

### 如果使用后台进程：

```bash
# 使用 deploy.sh 脚本（推荐）
./scripts/deploy.sh

# 或手动启动
nohup python -m uvicorn backend.api.main:app --host 0.0.0.0 --port 8000 > logs/api.log 2>&1 &

# 记录进程 ID
echo $! > /tmp/backend.pid

# 验证服务启动
sleep 5
curl http://127.0.0.1:8000/health
```

---

## 🔍 第七步：监控验证

**更新后至少观察 24 小时！**

### 1. 实时监控日志

```bash
# 监控主日志
tail -f logs/paper_push_*.log

# 监控 API 日志（如果有）
tail -f logs/api.log

# 监控系统日志
journalctl -u paper-push.service -f
```

### 2. 检查关键指标

```bash
# 查看最近的运行记录
sqlite3 data/database/paper_push.db "SELECT id, created_at, status, paper_count FROM runs ORDER BY created_at DESC LIMIT 5;"

# 查看错误日志
grep -i error logs/paper_push_*.log | tail -20

# 检查资源使用
top -p $(pgrep -f "python.*backend")
```

### 3. 验证定时任务

```bash
# 等待下一次定时任务执行
# 检查日志，确认任务正常运行

# 查看系统日志（crontab）
grep paper-push /var/log/cron

# 查看 systemd 日志
journalctl -u paper-push.service --since "1 hour ago"
```

### 4. 功能验证

- ✅ 数据抓取正常
- ✅ 评分算法正常
- ✅ AI 报告生成正常
- ✅ 推送功能正常
- ✅ Web 界面正常（如果有）

---

## ⚠️ 第八步：处理问题

### 常见问题及解决方案

#### 问题1：服务启动失败

**排查步骤**：
```bash
# 查看详细错误
tail -100 logs/api.log

# 检查端口占用
lsof -i:8000

# 检查依赖
pip list | grep <package-name>

# 手动启动查看错误
python -m uvicorn backend.api.main:app
```

#### 问题2：数据库错误

**排查步骤**：
```bash
# 检查数据库文件
ls -lh data/database/paper_push.db

# 验证数据库结构
sqlite3 data/database/paper_push.db ".schema"

# 检查数据库完整性
sqlite3 data/database/paper_push.db "PRAGMA integrity_check;"
```

#### 问题3：配置错误

**排查步骤**：
```bash
# 检查环境变量
cat .env

# 验证配置加载
python -c "from backend.core.config import Config; print(Config.DEEPSEEK_API_KEY[:10])"
```

#### 问题4：依赖冲突

**排查步骤**：
```bash
# 查看依赖版本
pip list

# 重新安装依赖
pip uninstall -y <package-name>
pip install <package-name>==<version>
```

---

## 🔙 回滚方案

如果更新后出现严重问题，立即回滚！

### 快速回滚步骤：

#### 1. 停止服务
```bash
# Docker
docker-compose down

# Systemd
sudo systemctl stop paper-push.timer

# Crontab
crontab -r

# 进程
pkill -f "python.*backend"
```

#### 2. 恢复代码
```bash
# 如果使用 Git
git reset --hard <previous-commit>

# 或者恢复备份
cd ..
tar -xzf /root/bio_backups/<timestamp>/bio_full_backup.tar.gz
```

#### 3. 恢复数据库
```bash
# 恢复数据库文件
cp /root/bio_backups/<timestamp>/paper_push.db data/database/

# 或者从 SQL 导入
sqlite3 data/database/paper_push.db < /root/bio_backups/<timestamp>/papers_dump.sql
```

#### 4. 恢复配置
```bash
cp /root/bio_backups/<timestamp>/.env ./
```

#### 5. 重启服务
```bash
# 按照"第六步：重启服务"的步骤操作
```

#### 6. 验证回滚成功
```bash
# 检查版本
git log -1

# 运行测试
python -m backend run --window-days 1

# 查看日志
tail -100 logs/paper_push_*.log
```

---

## 📝 更新后的最佳实践

### 1. 记录更新日志

创建 `UPDATE_LOG.md` 文件，记录每次更新：

```markdown
## 2024-01-XX 更新记录

**更新内容**：
- 优化了评分算法
- 修复了 PubMed 数据源的 Bug
- 添加了新的配置项

**执行人**：张三
**更新时间**：2024-01-XX 10:30
**是否成功**：是
**遇到的问题**：无
```

### 2. 定期备份

```bash
# 创建自动备份脚本
cat > scripts/auto_backup.sh << 'EOF'
#!/bin/bash
BACKUP_DIR=/root/bio_backups/$(date +%Y%m%d_%H%M%S)
mkdir -p $BACKUP_DIR
cp data/database/paper_push.db $BACKUP_DIR/
cp .env $BACKUP_DIR/
git log -1 > $BACKUP_DIR/version.txt
echo "Backup completed: $BACKUP_DIR"
EOF

chmod +x scripts/auto_backup.sh

# 添加到 crontab（每周备份）
# 0 2 * * 0 /path/to/bio/scripts/auto_backup.sh
```

### 3. 监控告警

```bash
# 设置告警脚本（示例）
cat > scripts/check_health.sh << 'EOF'
#!/bin/bash
# 检查服务是否正常
if ! curl -s http://127.0.0.1:8000/health > /dev/null; then
    echo "Backend service is down!" | mail -s "Alert: Bio Service Down" admin@example.com
fi
EOF
```

### 4. 灰度发布（可选）

对于重大更新，可以考虑：
1. 先在测试服务器上部署
2. 观察 1-2 天
3. 确认无问题后再部署到生产环境

---

## 📚 相关文档

- [初次部署指南](./服务器部署指南.md)
- [部署调整清单](./部署调整清单.md)
- [定时任务设置](./README_定时任务设置.md)
- [优化建议](./optimization.md)

---

## 🆘 紧急联系

如果遇到无法解决的问题：

1. 立即回滚到上一个稳定版本
2. 保存完整的错误日志
3. 联系开发人员
4. 不要在生产环境上随意尝试修复

---

## ✅ 更新检查清单（打印使用）

```
更新前：
□ 查看更新内容和变更记录
□ 记录当前版本号
□ 备份数据库
□ 备份配置文件
□ 备份代码
□ 停止运行中的服务

更新中：
□ 拉取/上传最新代码
□ 更新 Python 依赖
□ 更新配置文件
□ 运行数据库迁移（如需要）

测试：
□ 测试代码加载
□ 测试数据源连接
□ 手动运行一次完整流程
□ 检查日志无错误
□ 验证功能正常

重启：
□ 启动服务
□ 检查进程状态
□ 查看启动日志
□ 验证服务可访问

监控：
□ 实时监控日志（至少1小时）
□ 等待下次定时任务执行
□ 验证定时任务成功
□ 检查数据和报告
□ 记录更新日志

完成！
```

---

**记住：安全第一，备份最重要！**
